public class FontChoice {

	private void Details() {

		// a great programming typeface (font)
		// has clean letter shapes, distinguishable punctuation ; : < > . ,
		// and ensures that similar characters are easy to tell apart
		// The letter l, versus the number 1
		// The letter o, versus the number 0
		// Curly braces { } are distinguishable from parenthesis ( )

		// Developers prefer monospace fonts
		// find a definitive list of them at
		// https://hackingcpp.com/dev/coding_fonts.html

		// Cascadia Code typeface: Default for VS 2022 and supports ligatures.
		var x = 5;
		if (x <= 6) {	}
		if (x != 7) { }
		// Cascadia Mono typeface: the no ligature version.
		// Consolas typeface: Default for older versions of VS
	}
}